Just a quick GO program to check if a User supplied string
would fit into a Twitter post. If it is valid it will say
valid. If it is not valid it will tell the user so and give
the number of characters the user went over the Twitter limit. 